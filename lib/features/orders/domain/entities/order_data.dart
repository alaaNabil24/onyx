class OrderData {



  String ? orderNumber;
  String ? status;

  String ? statusId;
  String ? price;
  String ? date;

  OrderData({this.orderNumber, this.status, this.price, this.date , this.statusId});
}